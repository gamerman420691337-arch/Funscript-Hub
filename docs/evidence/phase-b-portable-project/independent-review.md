# P1a scoped independent review and repairs

This is a bounded source-review and regression record, not external approval or a proof of the entire program.

## Protocol source review

The independent reviewer identified six concrete counterexamples: truncated legacy recovery rejection/quadratic lookup; retained-history motion/protection mismatch; head older than a persisted revision; missing generated receipt evidence; inconsistent candidate commit/lineage relationships; and noncontiguous retained history positions.

The protocol owner verified them against source, observed six failing regressions on the draft, repaired them, and obtained 81 passing protocol tests including 15 package cases. The reviewer did not reread repaired protocol source.

## Actual worker integration

A real confined constrained-text worker completed generation, then package capture failed exact object-role validation. The manifest had no explicit reference for the raw program named by the worker receipt. Adding the paired original-program identity preserved exact required-role checking and original bytes, including shared-digest role unions. The separate real-worker closure test subsequently passed, including during the final combined gate.

This is deterministic synthesized generation, not local-assistant, neural, media-quality, or hardware qualification.

## Engine capture source review

The independent reviewer found three source-confirmed issues; the owner verified all three before repair.

- Legacy prefix SQL charged unrelated nested-ID project recovery rows before ownership filtering. A tiny authorized project with 100,000 unrelated rows reproduced ResourceExhausted. No foreign bytes were confirmed in the returned manifest. Exact SQL ownership now precedes accounting.
- A primary worker-source identity could disagree with the retained source catalog while being treated like an optional executable dependency. An inert hostile durable-state fixture reproduced acceptance with missing primary bytes. Exact catalog binding now rejects it; the absent-executable positive control remains accepted.
- Authored and attempt assembly repeatedly scanned candidate/revision vectors. Indexed lookups replace quadratic scans. A 10,000-candidate/10,000-revision/128-job-bucket fixture checks membership and order; it is not a speed benchmark.

The two behavioral regressions failed before repairs and passed afterward. The owner ran the four focused cases and existing closure fixtures; the root reran the combined final gate. Repaired capture source was not independently reread.

## Final disposition

Final local gate: 343 successful Rust test executions, zero failures. One default-ignored real-worker fixture was explicitly invoked and passed. Final source fingerprint: `4b3f30457e82aedc5de50ea211ac1d3089ba143009f791c34b228d57828215a4`.

External `review_commit` for f24dfca returned `Transport closed`; it produced no verdict. No universal closure, external review approval, user-facing portable operation, cross-platform qualification, or Good-phase completion is claimed.
